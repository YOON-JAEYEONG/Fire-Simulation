# 경로 탐색 작업 인계 — 2026-09-08 v2

## 2026-09-11 실행 폴더 복원

실행 위치는 `outputs/Fire-Simulation/YUFS/YUFS.uproject`이다. `204a8d3`의 소스·맵·에셋을 이 기존 폴더에 복원했으며, 이번 화재 연결 수정도 `JJW_NPC_BEHAVIOR`에 포함한다. 로컬 작업 브랜치와 GitHub 게시 브랜치는 모두 `JJW_NPC_BEHAVIOR`이며 main은 변경하지 않았다. 이전 맵에만 존재하고 `e514c56`과 내용이 일치하는 에셋 13개를 제거했다.

`Prototype`의 화재 볼륨에 `/Game/Fires/Fire/SparseVolumeMaterial_Inst`를 재연결해 저장했다. 연결된 텍스처는 `untitled_v1`이다. BIN은 기존 `Content/Fires/Scenario_01/smoke_data.bin`을 그대로 사용하며 Git에 포함하지 않으므로 새 클론에서는 별도로 배치해야 한다. 마지막 사용자 설정 전체를 자동 저장본으로 복구한 것은 아니다.

이 경로에서 Editor Development 빌드 성공 및 NPC 자동화 테스트 16개 성공, 실패 0개를 다시 확인했다. 복원 후 60초 NullRHI 맵 실행은 오류 없이 종료됐으며 20명 중 17명 대피, BIN Ready를 확인했다. 그래픽 출력이나 BIN/SVT 대응까지 검증한 것은 아니다. 이번 검증 파일은 작업 폴더의 `work/restored-*`에 있다. 아래 9월 8일 실행 수치는 당시 기록이다.

## 먼저 읽을 문서

- 현재 기능·설정·한계·검증: [YUFS/Docs/NPC_NAVIGATION_V2.md](YUFS/Docs/NPC_NAVIGATION_V2.md)
- 바이너리와 화면 에셋의 차이: [YUFS/Docs/NAVIGATION_HAZARDS.md](YUFS/Docs/NAVIGATION_HAZARDS.md)
- 9월 7일 이동 기반 작업: [YUFS/Docs/NAVIGATION.md](YUFS/Docs/NAVIGATION.md)

## 작업 기준과 보존 사항

사용자는 NPC 경로 탐색 담당이다. 모션과 문·도움 등 상호작용은 다른 담당자가 있지만, 이번에는 개인별 연기·고온 인지, 경보와 군중에 따른 대피 판단, 양보·대기·막힘 복구의 수정을 명시적으로 요청했다.

기준은 `IT_YOON_JAEYEONG@4293ca3`이며 게시 대상은 `JJW_NPC_BEHAVIOR`이다. **main에는 커밋하지 말 것.** 이전 JJW의 `e514c56` 및 `40f9548` 이력을 부모 커밋으로 보존하되, 프로젝트 내용은 IT 기준의 새 NPC 작업본을 사용한다. 이전 판단 코드와 새 판단 코드를 혼합하지 않는다.

9월 9일 게시 준비 중 기존 작업 폴더가 예전 JJW 파일들로 바뀐 것을 확인해 소스 백업과 수정 기록으로 별도 작업 사본을 만들었다. 기존 폴더는 보존했다. 사용자 에디터의 화재 머티리얼 연결 변경 파일은 해당 폴더에서 찾지 못했으므로 이번 게시본의 자산은 IT 기준이다. 필요한 경우 화재 볼륨에 `SparseVolumeMaterial_Inst`를 연결한다.

## 현재 구현

개인별 성향과 대피 판단, 가림을 고려한 연기·고온 감지, 개인별 위험 기억과 근거리 경고, 그 기억을 쓰는 비동기 경로 탐색, 통행 우선순위와 물리적 후퇴·측방 복구를 연결했다. ONNX 입력 배열은 28개 그대로이며 가중치는 변경하지 않았다. 새 입력을 모르는 기존 정책의 이동 결정을 실행부에서 상태에 맞게 제한한다.

`YUFSLocalMovementComponent`는 새 기본 컴포넌트다. 프로젝트를 재시작해 새 DLL과 컴포넌트를 로드할 것. `Prototype`을 에디터 안에서 실행한다.

## 검증

UE 5.7.4 Windows Development Editor 빌드 성공, 자동화 테스트 16개 성공(실패 및 테스트 경고 0).
최종 60초 Prototype SIE 검사에서는 20명 중 18명이 출구 도달, 2명은 계단 부근에서 이동 상태였다. 개인별 출발 시점, 양보·복구 전환을 로그로 확인했다. NullRHI와 별도 저장 폴더의 규칙 기반 정책으로 검사했으며 그래픽 재생 및 학습 모델 검증은 아니다.

작업 폴더의 `outputs/navigation-validation/2026-09-08-v2/`에 테스트 보고서, 빌드 로그, 맵 결과, 요약, NPC 실행 로그가 있다. v2 적용 전 소스 복사본은 같은 작업 폴더의 `work/before-individual-npc/`이다. 복사본은 Git 체크포인트가 아니며 복원할 때 이후 사용자 변경을 덮어쓰지 않도록 주의할 것.

## 다음 확인 사항

1. BIN과 SVT의 위치·시간·축·정규화 대응. 사용자는 팀원이 만든 데이터라 생성 정보를 모른다고 답했다. BIN: 8,000프레임, 153×115×17. SVT `untitled_v1`: 514프레임, 361×158×36. 대응을 추측해서 비율을 바꾸지 말 것.
2. 맵 검사에서 기록된 경로의 연기·열은 모두 0이었다. 실제 시각 효과 회피를 검증한 것으로 설명하지 말 것. 합성 데이터 기반 감지와 위험 경로 테스트는 통과했다.
3. 모든 밀집 상황의 교착 해소는 보장하지 않는다. 초기 캡슐 겹침, 좁은 문, NavMesh와 충돌 설정, 큰 NavMesh 다각형 안의 위험 우회 한계는 남아 있다.
4. 실시간 새 인지·교통 디버그 값은 기존 타임라인 기록 형식에는 추가하지 않았다. 대규모 군중에는 근거리 탐색의 공간 인덱스 최적화가 필요하다.
